# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL is used to connect to your Supabase database.
POSTGRES_URL="postgres://postgres.[USERNAME]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?workaround=supabase-pooler.vercel"

# App URL (used in emails and metadata)
APP_URL=http://localhost:3000

# App Configuration
NEXT_PUBLIC_APP_NAME="Acme Inc."
NEXT_PUBLIC_APP_SHORT_NAME="Acme"
NEXT_PUBLIC_APP_URL=http://localhost:3000

# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET='supersecret'

# Better Auth URL для backend-сервера (app-server)
# Важно: должен указывать на URL самого backend (например, http://localhost:7000)
# чтобы избежать ERR_TOO_MANY_REDIRECTS при проксировании запросов
BETTER_AUTH_URL=http://localhost:7000

# Шифрование паролей интеграций (Megafon FTP и др.) в БД.
# Если не задан — используется BETTER_AUTH_SECRET.
# Рекомендуется задать отдельный ключ: openssl rand -base64 32
# ENCRYPTION_KEY=

# Preconfigured Google OAuth provider, works out-of-the-box
# @see https://www.better-auth.com/docs/authentication/google
AUTH_GOOGLE_ID=''
AUTH_GOOGLE_SECRET=''
EMAIL_SANDBOX_ENABLED=true
EMAIL_SANDBOX_HOST=localhost

# AI провайдер: openai | openrouter | deepseek
AI_PROVIDER=openrouter
# Базовая модель (баланс качества/цены)
AI_MODEL=anthropic/claude-sonnet-4.6
# Премиум-модель (сложный reasoning, высокорисковые ответы)
AI_MODEL_PREMIUM=openai/gpt-5.4
# Модель для длинного контекста (большие документы/транскрипты)
AI_MODEL_LONG_CONTEXT=openai/gpt-5.4
# Дешевая быстрая модель (высокий throughput)
AI_MODEL_CHEAP=openai/gpt-5-nano

# API ключи (нужен ключ для выбранного провайдера)
OPENAI_API_KEY=
OPENROUTER_API_KEY=
DEEPSEEK_API_KEY=
# Отдельная модель для рекомендаций по звонкам (опционально)
# AI_RECOMMENDATIONS_MODEL=anthropic/claude-sonnet-4.6

# ASR (распознавание речи) — для транскрибации звонков
# Основной путь: Giga AM sync API, оркестрация шагов в Inngest
# Giga AM (HTTP API) — при GIGA_AM_ENABLED=true задайте GIGA_AM_TRANSCRIBE_URL
# (валидный HTTP(S) URL, рекомендуется базовый URL без пути, например: https://your-host.example)
GIGA_AM_ENABLED=false
GIGA_AM_ASYNC_MODE=true
# Тариф Giga AM для расчёта себестоимости (рублей за 1 секунду аудио)
GIGA_AM_RATE_RUB_PER_SECOND=0.01
GIGA_AM_TRANSCRIBE_URL=
# OPENAI_API_KEY — для LLM-нормализации текста (орфография, пунктуация)

# Megafon PBX FTP (для cron-задачи загрузки записей)
MEGAFON_FTP_HOST=records.megapbx.ru
MEGAFON_FTP_USER=your-megafon-ftp-user
MEGAFON_FTP_PASSWORD=your-megafon-ftp-password

# MinIO / S3-совместимое хранилище (для файлов и записей звонков)
# Локальный MinIO из docker-compose: minio / password, bucket: files.acme.com
# Yandex Object Storage: https://storage.yandexcloud.net, region: ru-central1
# Для Yandex SpeechKit ASR: статический ключ должен иметь права чтения объектов (storage.editor)
AWS_S3_ENDPOINT=http://localhost:9002
AWS_S3_FORCE_PATH_STYLE=true
AWS_ACCESS_KEY_ID=minio
AWS_SECRET_ACCESS_KEY=password
AWS_S3_BUCKET=files.acme.com
# AWS_REGION=us-east-1

# Inngest (cron и фоновые задачи)
INNGEST_API_URL=http://localhost:3001  # URL Inngest API для callback событий
# INNGEST_SIGNING_KEY=signkey-prod-...  # из Inngest dashboard
# INNGEST_EVENT_KEY=  # опционально, для отправки событий от Python сервисов

# Python Audio Enhancer (опционально, для продвинутой обработки аудио)
# Временно отключен по умолчанию - ухудшает качество звука
AUDIO_ENHANCER_ENABLED=false
# Если не задан URL — используется только FFmpeg (быстрее, но проще)
# Docker: http://audio-enhancer:8080
# Локально: http://localhost:8080
AUDIO_ENHANCER_URL=

# Speaker Diarization Service (опционально, для разделения спикеров)
# Docker: http://speaker-embeddings:8080
# Локально: http://localhost:8080
SPEAKER_EMBEDDINGS_URL=https://vnggncb-speaker-embeddings.hf.space/
SPEAKER_EMBEDDINGS_ASYNC_MODE=true

# Webhook Telegram использует APP_URL / NEXT_PUBLIC_APP_URL (тот же URL, что и приложение)